# Dependencies
node_modules/

# Runtime data — user's saved links live here; not source, don't commit it
data/links.json

# Logs & OS noise
*.log
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*

# The task brief itself — not part of the deliverable
*.pdf
